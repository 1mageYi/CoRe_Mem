project:
  name: core_mem_v31_latent
  stage: stage2

model:
  latent_objective:
    hidden_dim: 48
    latent_dim: 24
    latent_queries: 6

training:
  latent_objective:
    batch_size: 96
    lr: 0.0006
    num_train_epochs: 10
