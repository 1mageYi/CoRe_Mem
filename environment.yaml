name: core_mem
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytest
  - pyyaml
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu124
      - torch==2.5.1+cu124
      - accelerate
      - datasets
      - fsspec==2026.2.0
      - peft
      - sentence-transformers
